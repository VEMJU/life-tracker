{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "headers": [
    {
      "//": "HTML must never be cached. Every script and iframe inside index.html carries a ?v= cache-buster, but index.html itself holds those version numbers — so if IT is cached, the browser keeps loading the old versions of everything and no refresh helps. Always revalidate HTML; the versioned assets it points to can then be cached hard.",
      "source": "/(.*\\.html)?",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=0, must-revalidate" }
      ]
    },
    {
      "//": "Vendored libraries are immutable — a new version means a new file.",
      "source": "/vendor/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    }
  ]
}
